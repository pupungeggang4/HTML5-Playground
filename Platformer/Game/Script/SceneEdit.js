function loopEdit() {
    renderEdit()
}

function renderEdit() {
    renderInit()
    renderFieldInit()

    renderBarTop()
    renderBarLeft()
    renderBarRight()
    renderBarBottom()

    renderScreen()
}

function mouseUpEdit(pos, button) {
    if (button === 0) {
        if (menu === false) {
            if (state === '') {
                
            }
        }
    }
}

function keyDownEdit(key) {
}
