function loopBattle() {
    renderBattle()
}

function renderBattle() {
    renderInit()
    field.render()
    player.renderHand()
}

function mouseUpBattle(pos, button) {
    if (button === 0) {
        if (menu === false) {
            if (state === '') {
                
            }
        }
    }
}

function keyDownBattle(key) {
}
