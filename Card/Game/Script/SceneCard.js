function loopCard() {
    renderCard()
}

function renderCard() {
    renderInit()
    fillText('123', [0, 200])
    tempObj.render({x: 200, y: 200})
}

function mouseUpCard(pos, button) {
    if (button === 0) {
        if (menu === false) {
            if (state === '') {
                
            }
        }
    }
}

function keyDownCard(key) {
}
