function loopBattle() {
    renderBattle()
}

function renderBattle() {
    renderInit()

    if (state === 'start') {
        renderStart()
    }
}

function mouseUpBattle(pos, button) {
    if (button === 0) {
        if (menu === false) {
            if (state === 'start') {

            }
            
            if (state === '') {

            }
        }
    }
}
